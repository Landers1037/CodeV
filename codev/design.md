# CodeV UI 设计规范（侧边导航 + 浅色工作台）

## 设计目标
- 风格关键词：极简、柔和、专业、工具型、轻拟态
- 视觉气质：深色导航承载品牌识别，浅色内容区突出信息和操作
- 交互原则：减少装饰噪音，强化层级与分区，让用户快速定位功能和状态
- 一致性原则：圆角、阴影、描边、间距、动效统一复用，避免页面各自为政

## 原型提炼

### 图 1：导航与列表页
- 结构核心：左侧深色竖向主导航 + 中部浅色分组列表 + 右侧主工作区
- 导航特征：窄侧栏、图标优先、选中项使用圆角高亮底板、品牌 logo 固定在顶部
- 列表特征：卡片弱化、分组标题清晰、条目行高充足、信息以名称 + 状态 + 数量呈现
- 层级特征：通过背景明度、边框和局部阴影区分层级，而不是重色块堆叠

### 图 2：参数设置面板
- 结构核心：顶部轻量工具条 + 中间参数编辑区 + 右侧纵向分类切换
- 控件特征：段控件、滑杆、开关、输入框都使用统一圆角与浅阴影
- 面板特征：设置项按“标题 - 控件 - 预览”顺序组织，阅读路径自上而下
- 交互特征：当前选中的设置分类必须高亮，参数区保持高密度但不拥挤

## 全局布局
- 应用主结构改为三段式：左侧主导航、顶部拖拽控制区、右侧页面内容区
- 左侧主导航为全局一级导航，承载：首页、下载、终端、设置、关于、快捷操作
- 页面内容区采用浅灰背景，内部通过卡片、面板、分栏形成次级层级
- 标题栏系统按钮保留在右上区域，拖拽区不覆盖交互按钮
- 禁用系统标题栏，继续使用自绘窗口控制按钮

## 导航系统
- 主导航位置：由顶部菜单改为左侧固定侧栏
- 侧栏宽度建议：72px 到 76px；内部按钮保持垂直排布
- 顶部区域放置品牌 logo；中部放一级导航；底部放设置、关于、添加等辅助操作
- 导航按钮默认使用图标，hover 显示 Tooltip，必要时在内容区标题中补足文字语义
- 激活态采用高亮底板 + 轻阴影 + 图标/文字强化，不使用过重描边
- 左侧深色区域与右侧浅色内容区必须形成明确明暗对比

## 内容区布局
- 内容区背景使用偏暖或偏灰白色，避免纯白刺眼
- 页面主体优先采用“页面标题 + 摘要说明 + 操作区 + 主内容”的结构
- 列表页支持分栏布局：左侧分类或分组导航，右侧具体内容列表
- 设置页支持参数面板布局：参数分组、预览块、辅助说明分层排列
- 列表与表单都应保留足够留白，避免内容直接贴边

## 卡片与面板
- 主卡片：圆角 20px，浅背景，细边框，柔和阴影
- 次级卡片/列表项：圆角 14px 到 16px，弱阴影或无阴影，强调 hover 态
- 深色导航中的高亮卡片：圆角 18px，背景使用半透明亮面或低对比浅灰
- 弹窗与浮层：延续浅色面板风格，避免过重模糊和强发光

## 圆角与尺寸
- 主导航图标按钮：40px 到 44px，圆角 14px 或胶囊式高亮容器
- 一级卡片/大面板：20px
- 常规卡片、分组容器、弹窗：16px
- 输入框、下拉框、段控件、开关容器：12px 到 14px
- 小型按钮与标签：10px 到 12px

## 色彩与主题
- 主背景：低饱和灰白色，保持长时间使用的舒适度
- 主导航背景：接近黑色但不纯黑，保留质感和层级
- 文字颜色：正文使用中高对比深灰，说明文字使用中灰
- 强调色：使用暖红橙色或品牌强调色，用于选中、开关、滑杆、主按钮
- 边框颜色：低对比中性灰，更多承担分层作用而非切割作用
- 深色模式延续“深导航 + 深工作台”的层级关系，但需降低阴影、提升描边可见度

## 阴影与质感
- 阴影原则：短半径、低透明度、低扩散，营造“柔和浮起”而不是“悬空漂浮”
- 导航高亮块使用内外层混合质感：外层弱投影，内层轻微亮面
- 内容卡片 hover 时提升阴影和边框亮度，但幅度要小
- 按压态通过轻微缩放与阴影减弱体现，不使用明显位移跳动

## 列表页规范
- 首页列表改为“左侧分类导航 + 右侧工具列表”的信息架构
- 分类导航支持数量提示、选中高亮、分组标题和空态提示
- 工具列表项建议采用横向信息布局：图标、名称、说明、状态、路径、快捷操作
- 操作按钮可集中在列表项右侧，常用操作优先直出，其余收纳进更多菜单
- 状态信息尽量标签化表达，例如“已安装”“仅扫描”“可下载”
- 长路径必须截断显示，同时通过 Tooltip 或补充区域保留完整信息

## 设置页规范
- 设置页改为“右侧纵向分类导航 + 左侧参数编辑区”的工作台布局
- 参数编辑区优先使用面板化结构，每个分组有标题、说明、控件和必要预览
- 页签不再采用传统横向大标签堆叠，改用更接近原型稿的纵向分类切换
- 常用控件风格统一为浅底、细描边、柔和阴影、清晰激活态
- 需要选择单一模式的场景，优先使用段控件而不是原生下拉输入
- 开关、滑杆、输入框和预览块要在同一视觉系统下呈现

## 表单控件
- 输入框高度建议 40px 到 44px，内边距充足，边框对比适中
- 段控件采用浅灰底座 + 白色激活块，激活项带轻阴影
- 开关使用清晰的开/关状态色差，打开时采用强调色
- 下拉框、选择器与输入框保持统一高度和圆角
- 文本域保持面板化外观，避免浏览器默认粗糙边框
- 错误态使用柔和红色边框和浅色背景提示，不依赖颜色单独表达

## 动效规范
- 页面切换：淡入 + 轻微位移，时长 180ms 到 240ms
- 导航切换：高亮块平滑过渡，避免突然跳变
- 按钮 hover：150ms 到 200ms
- 按钮按压：80ms 到 120ms
- 弹窗：fade + zoom，小幅度缩放
- 禁止使用大弹跳、强旋转、强透视等破坏工具感的动画

## 交互与可用性
- 所有交互元素必须提供 hover、active、focus-visible、disabled 四种反馈
- 图标按钮必须提供 `aria-label` 和 Tooltip，不能只依赖图标猜测含义
- 侧边导航按钮点击热区不小于 44px
- 表单项需要有标题和辅助说明，不能仅使用 placeholder 充当标签
- 空状态、未安装状态、错误状态要提供可执行下一步，而不是只展示提示文案
- 列表与设置面板均需兼容明暗主题，不能只在浅色下可用

## Windows 细节
- 拖拽区域保留在顶部栏与品牌空白区，交互按钮必须设置为 `no-drag`
- 左侧导航不作为拖拽区，避免点击按钮时误触窗口拖动
- 窗口控制按钮风格需融入整体界面，hover 态清晰，关闭按钮保留风险色提示
- 关闭最小化到托盘、托盘退出等系统行为维持现有能力，但 UI 风格需统一
